package net.easyappsecurity.account.recovery.validation;

public enum ErrorType {
    INTERNAL_ERROR,
    INVALID_TOKEN,
    REGISTRATION_EXPIRED,
    ACCESS_DENIED,
    FORBIDDEN,
    USER_NOT_FOUND
}
